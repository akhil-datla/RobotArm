// RobotArm.h — umbrella header. The ONLY include a sketch needs:
//
//     #include <RobotArm.h>
//     using namespace roboarm;
//
// It pulls in every public class so a beginner includes nothing else. Populated
// as the library is built (see CLAUDE.md phased plan).
#pragma once

// (Public headers are added here phase by phase.)
